#!/bin/bash\n# Production Deployment Script for AI Model Sentinel v2.0.0\n\necho "🚀 Deploying AI Model Sentinel v2.0.0 to Production..."\n\n# 1. Install production dependencies\npip install -r requirements-production.txt\n\n# 2. Initialize production database\npython -c "from core.database import init_database; init_database()"\n\n# 3. Start with Gunicorn (Production WSGI Server)\necho "Starting Gunicorn production server..."\ngunicorn --bind 0.0.0.0:8000 --workers 4 --timeout 120 wsgi:app\n\n# Alternative: Start with uvicorn for ASGI\n# uvicorn web_interface.app:app --host 0.0.0.0 --port 8000 --workers 4\n