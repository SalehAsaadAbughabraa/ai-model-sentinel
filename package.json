{
  "name": "ai-model-sentinel",
  "version": "0.1.0-alpha.0",
  "description": "Lightweight open-source toolkit for monitoring AI models in production",
  "main": "index.js",
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "test": "lerna run test",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist",
    "clean:install": "npm run clean && npm install",
    "lint": "lerna run lint",
    "start": "lerna run start --parallel"
  },
  "keywords": [
    "ai",
    "ml",
    "monitoring",
    "machine-learning",
    "model-monitoring",
    "cloud",
    "aws",
    "azure",
    "gcp"
  ],
  "author": "Saleh Asaad Abughabraa",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "dependencies": {
    "@types/express": "^4.17.23",
    "express": "^5.1.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
